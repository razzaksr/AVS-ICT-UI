import { Decoration } from './decoration';

describe('Decoration', () => {
  it('should create an instance', () => {
    const directive = new Decoration();
    expect(directive).toBeTruthy();
  });
});
