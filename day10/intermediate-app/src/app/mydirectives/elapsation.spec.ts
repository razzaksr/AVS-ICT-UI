import { Elapsation } from './elapsation';

describe('Elapsation', () => {
  it('should create an instance', () => {
    const directive = new Elapsation();
    expect(directive).toBeTruthy();
  });
});
