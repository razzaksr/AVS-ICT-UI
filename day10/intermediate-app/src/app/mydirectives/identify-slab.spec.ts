import { IdentifySlab } from './identify-slab';

describe('IdentifySlab', () => {
  it('should create an instance', () => {
    const directive = new IdentifySlab();
    expect(directive).toBeTruthy();
  });
});
